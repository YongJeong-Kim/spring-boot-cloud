server:
  port: 9000
spring:
  application:    name: book-apigateway
  cloud:
    config:
      uri:      - http://localhost:8888
      
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/    