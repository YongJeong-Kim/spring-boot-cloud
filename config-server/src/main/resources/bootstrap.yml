server:
  port: 8888
    
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        encrypt.enabled: false
        git:
          uri: https://github.com/YongJeong-Kim/spring-boot-cloud/
          searchPaths: config-repo
